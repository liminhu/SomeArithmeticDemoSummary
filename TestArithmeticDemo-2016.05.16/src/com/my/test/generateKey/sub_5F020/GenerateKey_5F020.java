package com.my.test.generateKey.sub_5F020;

public class GenerateKey_5F020 {
	
	private int init_key_data(String data1, String data2, int length){
		String initStr="x4BBU560C12P537I5P0P";
		StringBuilder resultBuilder1=new StringBuilder();
		StringBuilder resultBuilder2=new StringBuilder();	
		StringBuilder  temp=new StringBuilder(initStr);
		int data1_len=data1.length();
		int initStr_len=initStr.length();
		if(data1_len-initStr_len>0){
			//for(int i=data1.length()-initStr.length(); i<data1.length())
		}
	
		return 0;
	}
	
	
	

}
